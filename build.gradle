apply plugin: 'java'
apply plugin: 'idea'

sourceCompatibility = 1.8
version = '1.0'

repositories {
    mavenCentral()
}

def build_number="asdf"

if (hasProperty("BUILD_NUMBER")) {
	build_number = project.getProperty("BUILD_NUMBER")
	jar.archiveName="ConfigurationAPI-${version}-${build_number}.jar"
}


sourceSets {
    main {
        java {
            srcDir 'src'
            exclude 'test'
       }
   }
}